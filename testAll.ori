Dev.changes(baseline, allBuilds.ori)

// Ignore noisy `pagefind` directory
→ (delta) => delta && Tree.map(delta, (project) =>
  Tree.mask(project, {
    ...Tree.constant(true)
    pagefind: false
  })
)
