Tree.map(projects.yaml, {
  // Add trailing slash so keys match the keys of the baseline folder
  key: (_, key) => Origami.slash.add(key)
  keyNeedsSourceValue: false
  value: (path) => Protocol.files(path)/build
})
